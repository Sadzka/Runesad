#include "Messenger.hpp"
#include "Utils/Sha512.hpp"

void Messenger::handleMessage(Server *server, MessageId id, sf::Packet &packet)
{
    switch (id) {
        default:
        {
            break;
        }
    }
}
